# Dylan8865Prac5

Practical 5 repository for demonstrating adaptive maintenance with GitHub Desktop-style branching, pull requests, merge, revert, and fix workflows.

Rollback state: configuration has been reverted to the stable legacy `config.txt` format while the JSON structure is fixed separately.
